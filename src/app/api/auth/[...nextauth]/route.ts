export { GET, POST } from '@/auth';

//  Usualmente  mão precisamos usar handlers de chamada dessa forma, pois damos preferencia às server actions
//  declaramos dessa forma quando precisamos que um servidor externo acesse nosso app programaticamente
// export async function GET() {
// }

// export async function POST() {
// }